
Наверно нужно создать сначала стандартную библиотеку для языка и посмотреть
что нужно будет доделать в самом языке.

Затем уже сделать игру с использованием SDL2.


Первая версия языка:
    есть классы
    есть GC
    есть базовый синтаксис

Синтаксис:
    type SomeClass {
        int    : Int;
        float  : Float;
        string : String;

        func me.init (
            int    : Int;
            float  : Float;
            string : String;
        ) {
            me.int    = int;
            me.float  = float;
            me.string = string;
        }
    }

Ага строки. Их нужно сделать в первую очередь. Как раз проверить работу GC.
В шарпе строки неизменяемые, и передаются по ссылке. Как сделать здесь?